import { style } from '@vanilla-extract/css';

export const wrapper = style({
  gap: 11,
  height: 50,
  paddingInline: 10,
  border: 0,
  boxShadow: 'none',
  borderRadius: 100
});

export const icon = style({
  scale: 0.8
});
